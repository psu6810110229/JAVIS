fastapi==0.115.12
httpx==0.27.2
pydub==0.25.1
pydantic==2.11.3
psutil==6.1.1
python-dotenv==1.1.0
SpeechRecognition==3.16.0
uvicorn[standard]==0.34.0

# ── Phase 4: Windows Machine Agent ────────────────────────────────────────────
pycaw==20240210          # Windows Core Audio API (volume, mute)
pywin32==308             # Win32 COM, SetForegroundWindow, Shell APIs
pyperclip==1.9.0         # Clipboard read/write
Pillow==11.2.1           # Screenshot via PIL.ImageGrab

# Windows 10/11 toast notifications (WinRT bridge)
windows-toasts==1.3.1

# ── Spotify Integration ───────────────────────────────────────────────────────
spotipy==2.24.0          # Spotify Web API SDK (OAuth PKCE)

# ── Web Search (free, no API key) ─────────────────────────────────────────────
duckduckgo-search==6.3.7  # DuckDuckGo instant answers + web/news results
python-docx==1.1.2        # Generate Microsoft Word .docx reports

# ── Native Windows TTS (Kokoro ONNX) ───────────────────────────────────────────
onnxruntime-directml==1.20.1  # ONNX with DirectML for Intel iGPU/CPU
kokoro-onnx==0.4.6           # Kokoro TTS engine
soundfile==0.13.1            # Audio file I/O
numpy==2.2.6                 # Required by kokoro-onnx
tzdata==2025.2               # IANA timezone database for zoneinfo on Windows
